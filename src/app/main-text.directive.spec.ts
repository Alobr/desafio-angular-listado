import { MainTextDirective } from './main-text.directive';

describe('MainTextDirective', () => {
  it('should create an instance', () => {
    const directive = new MainTextDirective();
    expect(directive).toBeTruthy();
  });
});
